create database demo;

use demo;

create table Class(
id int,
name varchar(100)
);

create table Teacher(
id int,
name varchar(100),
age int,
country varchar(50)
);

create table Students(
id int,
name varchar(100),
age int,
country varchar(50)
);

